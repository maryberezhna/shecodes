:root {
  --main-color: #ffb3b3;
  --second-color: #3b9ae1;
  --text-shadow-light: 1px 1px 2px pink;
}
.wrapper {
  max-width: 800px;
  margin: 50px auto;
  font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
}
h1 {
  color: var(--second-color);
  text-shadow: var(--text-shadow-light);
}
h2 {
  font-weight: normal;
}
.main_box,
.contact-box {
  display: flex;
  justify-content: center;
}
.contact-box {
  margin-top: 50px;
}
a {
  padding: 20px;
  border-radius: 10px;
  margin-right: 60px;
  background-color: var(--main-color);
  border: 2px solid var(--second-color);
  color: black;
  text-decoration: none;
}
.back {
  border: 2px solid red;
}
.main_box-about {
  padding-top: 40px;
}
img {
  border-radius: 50%;
  margin-right: 20px;
  margin-top: 20px;
}
